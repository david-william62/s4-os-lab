#include <stdio.h>

void main() {
  int n, p;
  printf("Enter the number of processes and resources: \n");
  scanf("%d", &p);
  scanf("%d", &n);

  // allocation and max matrices
  int alloc[r][p], max[r][p];
}
